fn main() {}
// ================================================================
// Unit test
// ================================================================
#[cfg(test)]
mod tests {
    use zench::bench;

    #[ignore = "tmp purpose"]
    #[test]
    fn test_x() {
        bench!(
                "x" =>{},
                "y" =>{},
                "z" =>{},

        )
        .report(|r| {
            r.title("value")
                .print();
        });

        // ----------------------------------------------------------------

        bench!(
                "x" =>{},
                "y" =>{},
                "z" =>{},

        )
        .report(|r| {
            r.title("value")
                .sort_by_median()
                .print();
        });

        // ----------------------------------------------------------------

        bench!(
                "x" =>{},
                "y" =>{},
                "z" =>{},

        );
    }
}
